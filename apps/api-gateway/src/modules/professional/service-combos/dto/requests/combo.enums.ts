export enum ServiceComboPricingModeDto {
  FIXED_PRICE = 'FIXED_PRICE',
  PERCENT_DISCOUNT = 'PERCENT_DISCOUNT',
}

export enum ServiceComboDurationModeDto {
  SUM_SERVICES = 'SUM_SERVICES',
  CUSTOM = 'CUSTOM',
}
